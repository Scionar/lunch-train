{
  "name": "lunch-train",
  "version": "1.0.0",
  "description": "Lunch group app.",
  "scripts": {
    "docker-production":
      "docker-compose -f docker-compose.production.yml up --force-recreate --build",
    "docker-development":
      "docker-compose -f docker-compose.development.yml up --force-recreate --build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Scionar/lunch-train.git"
  },
  "dependencies": {}
}
