import React from 'react';
import './empty-overview-note.css';

const EmptyOverviewNote = () => (
  <div className="empty-overview-note">
    <p>No trains created.</p>
    <p>Be the first to create!</p>
  </div>
);

export default EmptyOverviewNote;
